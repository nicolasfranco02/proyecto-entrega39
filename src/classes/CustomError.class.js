class CustomError {
    constructor(codigo, descripcion, detalles) {
        this.codigo = codigo;
        this.descripcion = descripcion ;
        this.detalles = detalles;
    }
}
export default CustomError;